<?php
    $hostname ="localhost";
    $username = "root";  // username database mysql
    $password = "";   // password database mysql
    $database = "medpath"; //nama database

    $connect =new mysqli($hostname, $username, $password, $database);
    
?>